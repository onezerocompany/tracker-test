name: Create Release

on:
  push:
    branches:
      - main

jobs:
  - name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Create Release
        uses: onezerocompany/coding/actions/release-issues@main
